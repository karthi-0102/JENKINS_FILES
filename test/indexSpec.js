const sinon = require('sinon')
const test = require('../index.js');

describe('index.js',()=>{

    describe('update',()=>{
        it('should update x',()=>{
            test.update();
           // sinon.assert(Index.x).to.be.equals(1);
        })
    })

})